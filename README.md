## Getting Started

First, run the development server:

```bash

npm install        // Install all dependencies

pnpm dev           // Run the project locally for development

npm run build      // Create a production build

npm start          // Run the production build to test the server environment


```
